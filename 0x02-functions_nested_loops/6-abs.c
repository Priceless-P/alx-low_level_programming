#include "main.h"

/**
 *  _abs - Check desciption
 *  Description: This function that computes the absolute value of an integer
 *  @r: Input value
 *  Return: 0 on success
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}
