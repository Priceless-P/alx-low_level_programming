#include "main.h"
#include <string.h>

/**
 * reset_to_98 -> takes a pointer to an int as parameter and updates the value
 * @n: value to be updated
 * Return: Always 0.
 */

int _strlen(char *s)
{
int a = _strlen(s);

printf("%d", a);
}