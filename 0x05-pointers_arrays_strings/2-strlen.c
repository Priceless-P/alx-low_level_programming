#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen -> returns the length of a string
 * @s: the string whose length will be will returned
 * Return: 0 on success
 */

int _strlen(char *s)
{
int a = strlen(s);

return (a);
}
